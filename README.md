

<!--question 1-->
1. (1)   getElementById is used to find a specific element by its ID. An ID cannot be used more than once , and this method works very fast."
(2)  
getElementsByClassName finds multiple elements. It updates itself automatically.


(3)  
"querySelector selects the first element from multiple matching elements." 

(4)  querySelectorAll can select multiple elements.

<!--Question 2-->
2.  The DOM process happens in three steps: first, create an element; then, add a class inside it; and finally, append it to the parent elemen


<!--question 3-->

3.  Event Bubbling is a process of rising step-by-step from bottom to top; in other words, the event flows from the child element toward the parent element
 
 <!--Question 4-->
 
  4. "Event Delegation is the process of connecting multiple child elements to a single parent element.


<!--question 5-->

  5. The preventDefault() and stopPropagation() methods are used for control: 
(1). preventDefault() stops the default behavior of an element.
(2). stopPropagation() stops the event bubbling